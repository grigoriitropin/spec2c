# Nix build result symlinks (regenerated by `nix build`, never committed).
# Default-deny: every entry here must be a justified, unavoidable build artifact.
# Everything else belongs tracked or must not exist — no scratch, no garbage.
result*
