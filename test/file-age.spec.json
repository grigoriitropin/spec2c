{
  "name": "file-age",
  "description": "Check if a JSON file's timestamp field is within a max age",
  "commands": {
    "check": {
      "description": "Check file timestamp freshness",
      "inputs": ["file", "max_age_seconds", "field_name"],
      "output": { "ok": "bool", "age_seconds": "number", "max_age_seconds": "number", "file": "string", "field": "string" }
    }
  },
  "core": {
    "file": "file_age_core.c",
    "function": "file_age_core"
  }
}
